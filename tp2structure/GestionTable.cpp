#include "GestionTable.h"


GestionTable::~GestionTable()
{
}

bool GestionTable::operator == (ClientEnAttente * client)
{
  // if (client->GetClient().nbPersonnes == nbPlaces_ && client->GetClient().sectionVoulu.interieur == laSection_.interieur)
}